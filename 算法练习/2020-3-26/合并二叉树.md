```js
mergeTrees = (t1, t2) => {
    let t;
    
    if(t1 && t2) {
        t = new TreeNode(t1.val + t2.val)
        t.left = mergeTrees(t1.left, t2.left);
        t.right = mergeTrees(t1.right, t2.right);
    } else {
        t = t1 || t2;
    }
    
    return t;
};
```

