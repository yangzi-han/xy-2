subtractProductAndSum = (n)=> {
    let digit=(n+'').length;
    let sum=0;
    let product=1;
    while(digit>0){
        let temp=parseInt(n/(Math.pow(10,digit-1)));
        sum+=temp
        product*=temp
        n=n-temp*(Math.pow(10,digit-1))
        digit--;
    }
    return product-sum;
};