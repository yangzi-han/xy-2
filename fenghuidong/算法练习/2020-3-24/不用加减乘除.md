```
add = (a, b) => {
    /**
     * 加法器
     * xor的值留守本位 and的值做进位
     */
    let p = (a & b) << 1
    let q = a ^ b
    while(p) {
        let _p = (p & q) << 1
        q = q ^ p
        p = _p
    }
    return q
};
```